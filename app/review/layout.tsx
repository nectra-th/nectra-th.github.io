import type { ReactNode } from "react";

export const metadata = { title: "Interaction Review — Grech Jewellers", robots: { index: false } };

export default function ReviewLayout({ children }: { children: ReactNode }) {
  return children;
}
